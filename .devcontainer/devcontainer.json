{
  "name": "MultiTranslator",
  "postCreateCommand": "if [ -n \"$CAPGOTOKEN\" ]; then npx --yes @capgo/cli login \"$CAPGOTOKEN\" && echo '✅ Capgo CLI auto-logged in'; else echo '⚠️  CAPGOTOKEN secret not set — capgo 명령 사용 시 수동 login 필요'; fi"
}
